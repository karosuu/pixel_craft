---
import { hrefFor, withBase, type Locale } from '../i18n/config';
import { t } from '../i18n/copy';

interface Props {
	locale: Locale;
}

const { locale } = Astro.props;
const copy = t(locale);
---

<section class="hero relative overflow-hidden pixel-grid">
	<div class="pointer-events-none absolute -left-24 top-10 h-64 w-64 rounded-full bg-cyan/10 blur-3xl"></div>
	<div class="pointer-events-none absolute right-0 top-24 h-72 w-72 rounded-full bg-magenta/10 blur-3xl"></div>
	<div
		class="relative mx-auto grid max-w-6xl items-center gap-10 px-4 py-20 sm:px-6 lg:grid-cols-[1.1fr_0.9fr] lg:py-28"
	>
		<div>
			<p class="hero-eyebrow text-xs font-semibold uppercase tracking-[0.2em] text-cyan">{copy.hero.eyebrow}</p>
			<h1 class="hero-title mt-4 text-4xl font-bold leading-tight tracking-tight sm:text-5xl lg:text-6xl">
				{copy.hero.titleBefore}
				{' '}
				<span class="gradient-text">{copy.hero.titleAccent}</span>
				{' '}
				{copy.hero.titleAfter}
			</h1>
			<p class="hero-body mt-6 max-w-xl text-lg text-muted">{copy.hero.body}</p>
			<div class="mt-8 flex flex-wrap gap-3">
				<a
					href={hrefFor(locale, 'contact')}
					class="glow-btn rounded-full px-5 py-3 text-sm font-semibold text-ink"
				>
					{copy.common.ctaPrimary}
				</a>
				<a
					href={hrefFor(locale, 'work')}
					class="hero-secondary btn-quiet rounded-full border border-line px-5 py-3 text-sm font-semibold text-fg hover:border-fg/40"
				>
					{copy.common.ctaSecondary}
				</a>
			</div>
		</div>
		<div class="flex justify-center lg:justify-end">
			<div class="relative flex w-full max-w-md items-center justify-center">
				<span class="pixel-speck pixel-speck-a" aria-hidden="true"></span>
				<span class="pixel-speck pixel-speck-b" aria-hidden="true"></span>
				<span class="pixel-speck pixel-speck-c" aria-hidden="true"></span>
				<img
					src={withBase('/logo-mark.png')}
					alt={copy.common.brandAlt}
					class="logo-float w-[min(100%,12.8rem)] sm:w-[min(100%,14.4rem)]"
					width="320"
					height="280"
				/>
			</div>
		</div>
	</div>
</section>
